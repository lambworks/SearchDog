# SearchDog

React mock UI for the SearchDog reconnaissance dashboard now lives in `SearchDogFrontMock.tsx`. Import that component anywhere in your React app to show the interactive front-end concept.
